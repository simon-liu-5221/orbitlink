# Frontend build-time config (Vite). Copy to .env for local dev if needed.

# Leave empty for local dev — Vite proxies /healthz and /api to localhost:8000.
# In production (Render static site) set this to the deployed backend origin,
# e.g. https://orbitlink-api.onrender.com
VITE_API_BASE_URL=
